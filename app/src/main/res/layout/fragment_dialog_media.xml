<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <import type="dev.gtcl.reddit.MediaType"/>
        <import type="android.view.View"/>
        <variable
            name="model"
            type="dev.gtcl.reddit.ui.fragments.media.MediaDialogVM" />
        <variable
            name="activityModel"
            type="dev.gtcl.reddit.ui.activities.MainActivityVM" />
    </data>

    <androidx.drawerlayout.widget.DrawerLayout
        android:id="@+id/drawer_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.coordinatorlayout.widget.CoordinatorLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <androidx.appcompat.widget.Toolbar
                android:id="@+id/top_toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                app:navigationIcon="@drawable/ic_back_arrow_surface_color_24dp"
                android:background="@drawable/transparent_gradient_bottom_to_top"
                android:elevation="1dp"
                android:visibility="@{activityModel.showUi &amp;&amp; !model.loading ? View.VISIBLE : View.GONE}">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:orientation="horizontal">

                    <TextView
                        android:layout_width="0dp"
                        android:layout_height="match_parent"
                        android:layout_weight="1"
                        android:text="@{model.mediaItems.size() > 1 ? String.format(&quot;%d/%d&quot;, model.itemPosition + 1, model.mediaItems.size()) : &quot;&quot;}"
                        tools:text="1/3"
                        android:textAppearance="@style/TextAppearance.AppCompat.Title"
                        android:gravity="center|start"
                        android:textColor="@android:color/white"/>

                    <ImageView
                        android:id="@+id/album_icon"
                        android:layout_width="wrap_content"
                        android:layout_height="match_parent"
                        android:src="@drawable/ic_collection_24dp"
                        android:padding="16dp"
                        android:tint="@android:color/white"
                        android:visibility="@{model.mediaItems.size() > 1 ? View.VISIBLE : View.GONE}"/>

                </LinearLayout>

            </androidx.appcompat.widget.Toolbar>

            <androidx.viewpager2.widget.ViewPager2
                android:id="@+id/viewpager"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:elevation="0dp"
                android:orientation="vertical"/>

            <ProgressBar
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:visibility="@{model.loading ? View.VISIBLE : View.GONE}"
                android:elevation="10dp"/>

            <include
                android:id="@+id/bottom_bar_controls"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                android:background="@drawable/transparent_gradient_top_to_bottom"
                layout="@layout/layout_media_buttons"
                app:hasPostUrl="@{model.post != null}"
                android:layout_gravity="bottom"
                android:visibility="@{activityModel.showUi &amp;&amp; !model.loading ? View.VISIBLE : View.GONE}"
                />

        </androidx.coordinatorlayout.widget.CoordinatorLayout>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/album_thumbnails"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            android:layout_gravity="end"
            android:orientation="vertical"
            />

    </androidx.drawerlayout.widget.DrawerLayout>


</layout>